/**
 * @see https://www.acmicpc.net/problem/2743
 * @description
 * # Title: 단어 길이 재기
 * ## Condition
 * ## Problem
 * 첫째 줄에 입력으로 주어진 단어의 길이를 출력
 * ## Input
 * 첫째 줄에 영어 소문자와 대문자로만 이루어진 단어 ((문자열 길이) <= 100)
 * ## Memo
 * 
 */

 const func = (str) => {
  return str.length;
};

/* 
  const fs = require('fs');
  const input = fs.readFileSync('dev/stdin').toString().trim();
  const result = solve(input);
  console.log(result);

  function solve(input) {

  }
 */
// const forSubmit = (input) => {
  
// }

module.exports = [ func ];
